# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its compilers focus without claiming live deployment or external usage.

## Cases

- `baseline`: `IR pressure`, score 168, lane `ship`
- `stress`: `lowering drift`, score 153, lane `ship`
- `edge`: `stack depth`, score 197, lane `ship`
- `recovery`: `diagnostic reach`, score 203, lane `ship`
- `stale`: `IR pressure`, score 224, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
